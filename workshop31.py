# function คือ การทำงา่นหนึ่งๆ จะไม่ทำงานหากเราไม่เรียกใช้ ( call function) 

# ที่มาที่ไปของ Function 
# 1. Built - in/standard Function เป็นฟังชั่นที่มากับตัวภ่าษา
# ไม่ต้องรุ้ว่าตัวฟังชั่นมันเป็นยังไง สนใจแค่ทำอะไร แล้วใข้ยังไง เช่น print() input() str() int() float() 
# 2. User define Function เป็นฟังชั่นที่  Dev เขียน / สร้างขึ้นมาเอง
# Third party Function เป็นฟังชี่นที่คนอื่นสร้างขึ้นแล้ว dev เอามาใช้ โดยไม่ต้องรุ้ว่าตัวฟังขั่นมันเป็นยังไง

# function คือ การทำงา่นหนึ่งๆ จะไม่ทำงานหากเราไม่เรียกใช้ ( call function) 

# ที่มาที่ไปของ Function 
# 1. Built - in/standard Function เป็นฟังชั่นที่มากับตัวภ่าษา
# ไม่ต้องรุ้ว่าตัวฟังชั่นมันเป็นยังไง สนใจแค่ทำอะไร แล้วใข้ยังไง เช่น print() input() str() int() float() 
# 2. User define Function เป็นฟังชั่นที่  Dev เขียน / สร้างขึ้นมาเอง
# Third party Function เป็นฟังชี่นที่คนอื่นสร้างขึ้นแล้ว dev เอามาใช้ โดยไม่ต้องรุ้ว่าตัวฟังขั่นมันเป็นยังไง

# ประเภทของ function มี4ประเภท
# 1. no parameter no return
# 2. have parameter no return 
# 3. no parameter have return
# 4. have parameter have return